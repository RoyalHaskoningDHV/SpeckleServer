apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "speckleserver.fullname" . }}-test-connection"
  labels:
    {{- include "speckleserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "speckleserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
